# Local Claude overrides
CLAUDE.local.md

# Local machine launch runbook (per-machine, not part of the deliverable)
docs/fleet-launch-winbash.md

# Per-service local config (secrets) — template is config-example.yaml
cmd/config.yaml
**/cmd/config.yaml

# Env files
.env
**/.env

# Build output
bin/
**/bin/

# Planning / spec inputs (not part of the deliverable)
claude-code-build-plan.md
backend-structure-template.md

# Go
*.exe
*.test
*.out
vendor/
coverage.out

# Node / frontend
node_modules/
**/node_modules/
frontend/dist/
frontend/coverage/

# real env files for deploy (templates *.env.example are committed)
deploy/env/*.env
!deploy/env/*.env.example
