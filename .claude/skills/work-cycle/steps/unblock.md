Make PR #N mergeable again so GitHub's auto-merge can land it — it is self-reviewed/conflicting but stuck (behind `main`, conflicting with `main`, or auto-merge was never actually armed). You claimed it. This is a MECHANICAL re-sync, NOT a review (single-agent mode — it is your own PR; no approval happens here either way). Print `[UNBLOCK] #<N> start`. GitHub via the **GitHub MCP** (`mcp__github__*`, owner `pizdagladki`, repo `full`); git/`gh` via Bash.

1. Read fresh state: `gh pr view <N> --json mergeStateStatus,mergeable,autoMergeRequest,headRefName,baseRefName,isDraft` (Bash) + `mcp__github__pull_request_read` (method=get, #N). If it is now a draft, closed/merged, or already `CLEAN`/`UNSTABLE` AND `autoMergeRequest` is set (it is progressing on its own) → nothing to do: release your claim (remove yourself from assignees, `mcp__github__issue_write` method=update #N), print `[UNBLOCK] #<N> already-progressing`, exit. `UNKNOWN` mergeStateStatus → GitHub is still computing it; re-read once after a short pause, and if still `UNKNOWN` release the claim and exit `[UNBLOCK] #<N> state-unknown` (a later cycle retries).

2. **Behind `main` without conflicts** (`mergeStateStatus == BEHIND` and `mergeable != CONFLICTING`):
   - `gh pr update-branch <N>` (Bash) — this MERGES the latest `main` into the PR branch with a merge commit; NO force-push. Do NOT pass `--rebase` (it rewrites history and requires a force-push, which branch protection/rulesets should block), and do NOT rebase locally.
   - A clean `main`-merge brings in only already-reviewed `main` code, so **keep** any `reviewed-armed` label: auto-merge stays armed and lands on green, and a semantic break in the clean merge is caught by CI (the `test` job re-runs on the new head; a red result is then picked up by select.md CI-RECOVERY). Do NOT send a clean re-sync back through REVIEW — that is needless churn.
   - Print `[UNBLOCK] #<N> updated-branch`, go to step 4.

3. **Real CONFLICT with `main`** (`mergeable == CONFLICTING` / `mergeStateStatus == DIRTY`):
   - `git fetch origin`; worktree on the PR branch at its latest remote head: `git worktree add ../<branch> <branch>` then `git -C ../<branch> reset --hard origin/<branch>`.
   - **Merge** (never rebase) `origin/main` in: `git -C ../<branch> merge origin/main`. Delegate the conflict resolution to the `coder` subagent (Sonnet) — pass it the worktree path, the PR's zone (its `services/<svc>` or `frontend`), and the conflicting files; it resolves preserving BOTH sides' intent (NO scope expansion), keeps `make test`/`cover` (≥80%)/`lint` green, and commits the merge. Do NOT hand-edit code.
   - If the coder cannot produce a clean, green merge → apply `needs-human` (`mcp__github__issue_write` method=update #N), comment what conflicts and why it is not mechanically resolvable, release your claim, print `[UNBLOCK] #<N> conflict-unresolvable -> needs-human`, exit.
   - `git -C ../<branch> push` (plain — it is a merge commit on top of the remote branch head, so the push is fast-forward; no `--force`). A resolved CONFLICT means the coder wrote NEW, unreviewed code, so **strip `reviewed-armed`** if present (`mcp__github__issue_write` method=update #N) AND **disarm auto-merge** (`gh pr merge <N> --disable-auto`) — holding the invariant (label present ⟺ auto-merge armed) so the resolved diff cannot merge on green before a fresh REVIEW re-reviews it (select.md keys re-review on the absent label). Print `[UNBLOCK] #<N> conflict-resolved`, go to step 4.

4. Ensure auto-merge is (still) armed ONLY when the PR stays `reviewed-armed` (the clean-`BEHIND` path): if the label is present and `gh api repos/pizdagladki/full/pulls/<N> -q .auto_merge` prints `null` → arm via GraphQL (the auto-mode classifier can deny `gh pr merge --auto` on your own PR as "self-approval" — use the mutation instead): `nid=$(gh api repos/pizdagladki/full/pulls/<N> -q .node_id) && gh api graphql -f query='mutation($id:ID!){enablePullRequestAutoMerge(input:{pullRequestId:$id, mergeMethod:SQUASH}){pullRequest{number}}}' -f id="$nid"` and VERIFY (`.auto_merge` now non-`null`; "already merged" = success); if arming fails → `CYCLE_ERROR unblock automerge-arm-failed #<N>`, exit. (Auto-merge fires only once the branch is up-to-date AND CI is green AND threads are resolved.) On the conflict-resolved path you stripped `reviewed-armed`, so do NOT re-arm here — REVIEW re-arms after it re-reviews. Do NOT touch the round counter or the board.

5. RELEASE your claim: remove yourself from assignees (`mcp__github__issue_write` method=update #N). If you stripped `reviewed-armed` (conflict-resolved), a fresh REVIEW process re-reviews and re-arms the new head. Print `[UNBLOCK] #<N> done`, exit.
