Address PR #N per the requested changes (label `needs-work`). Print `[ADDRESS] #<N> start`. (Single-agent mode: it is your own PR; the review comments were left by a fresh review.md process or by the CI-RECOVERY clause, and read as a self-contained spec.)

1. Fetch context via MCP: `mcp__github__pull_request_read` (method=get, #N) + `mcp__github__pull_request_read` (method=get_diff, #N — the diff) + `mcp__github__pull_request_read` (method=get_comments, #N — read ALL review comments). This is your only context — memory lives in GitHub. These are the **reviewer's own** comments — they already fold in any applied Copilot findings, and the reviewer already adjudicated + resolved Copilot's threads, so do NOT separately chase Copilot threads. Any new Copilot comments on your push are adjudicated in the NEXT review round.
2. **Sync first:** `git fetch origin`. Work in a worktree on the PR branch at its **latest remote head** — another agent may have pushed since the review: `git worktree add ../<branch> <branch>` then `git -C ../<branch> reset --hard origin/<branch>`. If it is behind `origin/main` → **merge** `origin/main` into the branch (`git -C ../<branch> merge origin/main`), have the `coder` resolve any conflicts, re-run the gates. Do NOT rebase — a rebase rewrites history so the push would need `--force`, which `settings.json` denies and branch protection blocks; a merge keeps the push fast-forward.
3. **Delegate the fixes to the `coder` subagent** (Sonnet): pass it the absolute worktree path, the zone (the PR's `services/<svc>` or `frontend`), and the reviewer comments (exactly what to change). It makes the changes strictly per the comments (NO scope expansion), keeps the gates green, and commits in the worktree. Do NOT write code yourself.
4. Confirm the coder returned green with its `make test`/`cover` (≥80%)/`lint` output. If it cannot reach green, send back the specific gap — do NOT hand-edit code.
5. Resolve the discussed comments, push (Bash).
6. Remove `needs-work` AND, if present, `reviewed-armed` — `mcp__github__issue_write` method=update on #N with the new label set; if you cleared `reviewed-armed`, also **disarm auto-merge** (`gh pr merge <N> --disable-auto`) to hold the invariant (label present ⟺ auto-merge armed). Clearing `reviewed-armed` is mandatory: your push produced a NEW diff that has not been self-reviewed, so the PR must go back through REVIEW (select.md keys re-review on the absence of `reviewed-armed`) and must not auto-merge on its first green CI before that. Do NOT touch the round counter. (The board is automated — do not touch it.) Print `[ADDRESS] #<N> pushed, back-to-review`.
7. Exit.
