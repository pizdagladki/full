# deploy/docker-compose.yml — services are added as they are created (Phase C+).
# Base dependency: MongoDB (the Go service canon uses mongo-driver).
services:
  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
