// Package service holds the ratings service business logic (orchestrating
// repositories and ELO calculation, levels, and match history). Service
// interfaces are added here by downstream resource slices via the new-resource
// skill.
package service
