// Package domain holds the store service domain models, DTOs, and enums. Entity
// types (catalog items, purchases, inventory entries) are added here by
// downstream resource slices via the new-resource skill.
package domain
