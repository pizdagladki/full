// Package middleware holds the store service cross-cutting request logic. The
// RequireAuth middleware (Redis-session validation) is added here by the auth
// integration slice via the new-resource skill.
package middleware
