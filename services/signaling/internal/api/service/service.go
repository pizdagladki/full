// Package service holds the signaling service business logic (signaling
// orchestration: SDP/ICE offer-answer relay and server-side time arbitration).
// Service interfaces are added here by downstream resource slices via the
// new-resource skill.
package service
